<UserControl x:Class="olbaid_mortel_7720.MVVM.View.LevelWrapperView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:olbaid_mortel_7720.MVVM.View"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
  <Grid>
    <Viewbox x:Name="PlayerhealthViewbox"
              Height="50"
             VerticalAlignment="Top">
            <ContentControl Content="{Binding PlayerHealthbar}" 
                            Height="{Binding RelativeSource={RelativeSource AncestorType=Viewbox}, Path=Height}"   />
    </Viewbox>

    <Viewbox>
      <ContentControl Content="{Binding CurrentLevel}" />
    </Viewbox>

    <ContentControl Content="{Binding PlayerView}" />

    <ContentControl Content="{Binding EnemyView}" />
  </Grid>
</UserControl>
